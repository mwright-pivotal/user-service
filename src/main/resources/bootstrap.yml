spring:
  profiles.active: local
  application:
     name: user-service
  cloud:
    kubernetes:
      config:
        name: user-service
        namespace: cf-workloads   
info:
  build:
    group: ${group}
    name: ${name}
    description: ${description}
    version: ${version}

